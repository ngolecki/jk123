blabla test
change on master
new change

now polling only from jenkins
aws security http closed, only my IP

aws security http any
github with git plugin

no polling in jenkins
aws http any.
build when a change is push on github
change url /build

